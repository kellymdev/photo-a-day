<h2>Add New '<%= @subject.name %>' Photo</h2>

<%= render partial: 'subjects/display_errors', locals: { object: @photo } %>

<%= form_for([@subject, @photo]) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>

  <%= f.submit 'Add Photo' %>
<% end %>
