<h2><%= @category.name %></h2>

<ul>
  <% @category.subjects.each do |subject| %>
    <li><%= link_to(subject.name, subject_path(subject)) %></li>
  <% end %>
</ul>

<p><%= link_to('Add a Subject', new_subject_path) %></p>

<p><%= link_to('Back to Category List', categories_path) %></p>